{
  "name": "ipcdj-worship-push",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute DB --local --file=./schema.sql --yes",
    "db:remote": "wrangler d1 execute DB --remote --file=./schema.sql --yes",
    "db:create": "wrangler d1 create ipcdj-worship-push --binding DB --update-config"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "2.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
