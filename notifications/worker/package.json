{
  "name": "ipcdj-worship-push",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute IPCDJ_PUSH_DB --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute IPCDJ_PUSH_DB --remote --file=./schema.sql"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "2.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
